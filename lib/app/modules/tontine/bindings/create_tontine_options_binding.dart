
import 'package:get/get.dart';

class CreateTontineOptionsBinding extends Bindings {
  @override
  void dependencies() {
    // For now, no dependencies are needed for the options screen.
    // This can be updated later if a controller is added.
  }
}
